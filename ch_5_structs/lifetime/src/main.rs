struct User {
    active: bool,
    username: &str,
    email: &str,
    sign_in_count: u64,
}

fn main() {
    let user1 = User {
        active: true,
        username: "someusername123",
        email: "someone@example.com",
        sign_in_count: 1,
    };
}

// compiler complains need lifetime specifier because struct needs to either own all the data or know the data will
// always be around, but references mean they do not own