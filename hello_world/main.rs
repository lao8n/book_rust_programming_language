fn main() {
    println!("Hello, world!"); // ! calls a rust macro
}

// compile with rustc main.rs
// run with .\main.exe
// .pdb file is created for debugging only on windows

// rust is ahead-of-time meaning it compiles to machine code and doesn't require rust to be installed to run